#!/usr/bin/python3
import random, string

pituus = int(input("Kuinka pitkän salasanan haluat? "))

merkit = string.ascii_letters+string.digits

salasana = "".join(choice(merkit)

for x in range(pituus):
    password.append(random.choice(merkit)


print(salasana)
